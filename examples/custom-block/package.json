{
  "name": "@cw/example-custom-block",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Add a new canvas-drawn document block type (a bar chart) via the block-type registry (registerBlockType) — no build step, browser import map."
}
