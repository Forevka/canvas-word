{
  "name": "@cw/example-ribbon-customization",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Customize the ribbon (reorder/remove built-ins, add tabs/buttons, macros + popups) via the customizeRibbon option — no build step, browser import map."
}
