node_modules/
dist/
*.tsbuildinfo

# local agent state / scratch
.omc/
shots/
.playwright-mcp/
*.docx
# exported / sample PDFs (scratch output, never commit)
*.pdf

# local Postgres data volume (docker-compose) and env secrets
/local-db-data/
.env

# large local test documents (real fixtures live in src/**/__fixtures__/)
# un-anchored so copies under any workspace dir are ignored too
RenderedReportsDocx_*

# Office lock files
~$*
frontend/dist-lib/
frontend/dist-node/
.codegraph/

# Tauri desktop build output (Rust target + generated schemas)
desktop/src-tauri/target/
desktop/src-tauri/gen/
# Raw DevTools perf traces (large; analysis lives in perf/baseline.md)
perf/*-trace.json
perf/keystroke-burst-*.json

# ClearScript bindings — throwaway output from frontend/scripts/{dump,compare}-pdfs.mjs
out-node*/
out-cs*/
*.out.txt
# per-worktree agent brief (pipeline cruft, never commit)
TASK.md
