# Local dev: the dashboard (5174), backend (8787), and editor (5173) run on
# separate origins. In production these are unset and the app falls back to
# location.origin (single-origin reverse proxy). See src/api.ts.
VITE_BACKEND_URL=http://localhost:8787
VITE_EDITOR_URL=http://localhost:5173
